C (шифротекст)
  │
  ▼ (убираем P)
c_hat = C * P^-1
  │
  ▼ (исправляем ошибку через H)
c_hat_fixed = исправленное кодовое слово
  │
  ▼ (декодируем Хэммингом)
m_hat = M * S
  │
  ▼ (убираем маску S^-1)
M = исходный 4-битный блок


Смысл алгоритма

McEliece — это асимметричная криптосистема, то есть у каждого есть публичный ключ для шифрования и секретный ключ для расшифрования.

Главная идея:

Берём линейный код с эффективным декодированием (например, код Хэмминга или код Гоппы).

Маскируем его секретными матрицами 
𝑆
S и 
𝑃
P, чтобы код выглядел как «случайный линейный код» → это публичный ключ.

Сообщение шифруется через этот публичный ключ и добавляется искусственная ошибка 
𝑍
Z.

Расшифровать без секретного ключа — это сложно, потому что декодирование произвольного линейного кода NP-трудно.

То есть McEliece строится на математической сложности задачи декодирования линейного кода.

2️⃣ На чём основан

Основной «тяжёлой» задачей является декодирование линейного кода с произвольной матрицей:

найти 
𝑀
 из 
𝐶
=
𝑀
⋅
𝐺
′
+
𝑍
найти M из C=M⋅G
′
+Z

Без знания 
𝑆
S, 
𝑃
P и исходного кода 
𝐺
G — невозможно эффективно исправить ошибки.

В основе стоят линейные алгебраические коды (Hamming, Goppa), теории исправления ошибок и случайные перестановки.

Особенность: в отличие от RSA или эллиптической криптографии, защита не основана на факторизации или дискретном логарифме, а на счётной сложности декодирования.

3️⃣ Для чего используется

Шифрование сообщений: секретные данные шифруются так, что без секретного ключа восстановить исходное сообщение невозможно.

Аутентификация: можно доказать, что сообщение создано владельцем секретного ключа.

Квантовая устойчивость: McEliece устойчив к атакам квантовых компьютеров (в отличие от RSA, DH, ECDSA).

4️⃣ Основные преимущества

Очень быстрое шифрование и дешифрование на больших ключах.

Защита растёт быстрее с увеличением длины ключа.

Устойчива к квантовым атакам.

5️⃣ Краткая схема работы

Генерация ключей:

Секретные: 
𝑆
,
𝐺
,
𝑃
S,G,P

Публичный: 
𝐺
′
=
𝑆
⋅
𝐺
⋅
𝑃
G
′
=S⋅G⋅P

Шифрование:

𝐶
=
𝑀
⋅
𝐺
′
+
𝑍
C=M⋅G
′
+Z

Дешифрование:

Размаскировка: 
𝐶
⋅
𝑃
−
1
C⋅P
−1

Исправление ошибок через код 
𝐺
G

Убираем маску 
𝑆
−
1
S
−1
 → получаем 
𝑀
M

Если хочешь, я могу сделать схематичную картинку, как McEliece шифрует и расшифровывает сообщение, с блоками 
𝑀
,
𝐺
′
,
𝑍
M,G
′
,Z и ключами 
𝑆
,
𝑃
S,P. Это наглядно покажет смысл алгоритма.

Хочешь, чтобы я её сделал?